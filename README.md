# Neuraldesk
A RAG based app which runs entirely on browser and no backend ,You can upload a file and ask anything about it .The app finds the most  relevant parts and sends them to a local LLM to generate an answer.
